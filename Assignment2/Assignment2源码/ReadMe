flex.l为词法分析源文件、bison.y为语法分析器源文件，TokenType.h为节点类型枚举定义，AST.h与AST.c为多叉树以及语法树节点定义
Mysemantic.h与Mysemantic.c为

输入make命令，生成可执行文件parser。

输入make clean，仅保留源文件。

test对应指导书上的17个测试用例。

./Test.sh 执行make，进行17个测试，打印语义检测结果与符号表；然后删除源文件外的生成文件。